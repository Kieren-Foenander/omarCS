[project]
name = "omarcs"
version = "0.1.0"
description = "Local CS2 match analysis for the Omarchy shell"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "awpy==2.0.2",
]

[project.scripts]
omarcs = "omarcs.cli:main"

[dependency-groups]
dev = [
  "pytest>=8.3,<9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

