This crate regenerates CS2 item maps from a local GameTracking-CS2 tree.

Place that tree beside this crate as `GameTracking-CS2/`, then:

```
python update_protos.py
```

Plugin users do not run this. The generated sources are already vendored.
